{
  "private": true,
  "name": "purescript-regl",
  "scripts": {
    "build": "pulp browserify --no-psa --include examples --to dist/main.js"
  },
  "dependencies": {
    "http-server": "^0.10.0",
    "light-server": "^2.2.1",
    "purescript-image-decoder": "github:jacereda/purescript-image-decoder#master",
    "regl": "^1.3.0",
    "serve-static": "^1.12.3"
  }
}
